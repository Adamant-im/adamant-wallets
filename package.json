{
  "version": "2.5.0",
  "devDependencies": {
    "@prettier/plugin-xml": "^2.2.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.1.0",
    "prettier": "^2.8.3"
  },
  "lint-staged": {
    "*.{json,svg,vue}": "prettier --write"
  },
  "scripts": {
    "prepare": "husky install"
  }
}
